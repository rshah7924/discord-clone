import React from "react";
import PlusIcon from "@material-ui/icons/Icon";

function Server() {
  return (
    <div className="server">
      <div className="server__channels">
        <PlusIcon style={{ color: green[500] }}>Add</PlusIcon>
      </div>
    </div>
  );
}

export default Server;
